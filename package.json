{
	"name": "kalo-ai",
	"private": true,
	"version": "0.0.1",
	"packageManager": "bun@1.3.14",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"prepare": "bun run --filter @kalo-ai/plugin-sdk build && bun run --filter @kalo-ai/plugin-example build && bun run --filter @kalo-ai/web prepare",
		"dev": "bun run --filter @kalo-ai/plugin-sdk build && bun run --filter @kalo-ai/plugin-example build && bun run --filter @kalo-ai/web dev",
		"build": "bun run --filter @kalo-ai/plugin-sdk build && bun run --filter @kalo-ai/plugin-example build && bun run --filter @kalo-ai/web build",
		"preview": "bun run --filter @kalo-ai/web preview",
		"check": "bun run --filter @kalo-ai/plugin-sdk build && bun run --filter @kalo-ai/plugin-example build && bun run --workspaces check",
		"check:watch": "bun run --filter @kalo-ai/web check:watch",
		"test": "bun run --filter @kalo-ai/web test",
		"fmt": "biome check --write",
		"deploy": "bun run build && wrangler deploy"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.7",
		"typescript": "^6.0.3",
		"wrangler": "^4.120.0"
	}
}
