# Shared packages

Reusable packages for the Kalo AI monorepo belong in this directory. Each package should have its own `package.json` and is discovered by the root Bun workspace pattern `packages/*`.
